<body>
  <h1>Instagram</h1>
  <% @posts.each do |post| %>
  <div>
    <%= link_to (image_tag post.image.url(:medium)), post_path(post) %>
    <br>
    <%= post.caption %>
  </div>
  <% end %>
</body>
